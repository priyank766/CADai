# CADai Frontend -- environment configuration
# Copy to .env.local to override. All vars must be prefixed with VITE_.

# Backend API base URL (defaults to http://localhost:8000/api if unset)
VITE_API_URL=http://localhost:8000/api
